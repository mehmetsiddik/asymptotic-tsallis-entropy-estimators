# Data folder

Place the simulation output table in this folder if the figures need to be regenerated from raw or summarized results.

Expected file name:

```text
asymptotic_tsallis_results.csv
```

Required columns:

```text
dist, d, estimator, n, var, z
```
